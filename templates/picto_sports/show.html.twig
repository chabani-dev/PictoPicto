{% extends 'base.html.twig' %}

{% block title %}PictoSports{% endblock %}

{% block body %}
    <h1>PictoSports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_sport.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_sport.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_sport.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_sport.updateAt ? picto_sport.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_sports_index') }}">Retour à la liste des PictoSports</a>

      <a href="{{ path('app_picto_sports_new') }}">Create new PictoSports</a>

    {# <a href="{{ path('app_picto_sports_edit', {'id': picto_sport.id}) }}">edit</a>

    {{ include('picto_sports/_delete_form.html.twig') }} #}
{% endblock %}
