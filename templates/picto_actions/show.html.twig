{% extends 'base.html.twig' %}

{% block title %}PictoActions{% endblock %}

{% block body %}
    <h1>PictoActions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_action.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_action.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_action.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_action.updateAt ? picto_action.updateAt|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_actions_index') }}">Retour à la liste des PictoActions</a>

   <a href="{{ path('app_picto_actions_new') }}">new Picto</a>

    {# {{ include('picto_actions/_delete_form.html.twig') }} #}
{% endblock %}
