{% extends 'base.html.twig' %}

{% block title %}PictoPersonnes{% endblock %}

{% block body %}
    <h1>PictoPersonnes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_personne.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_personne.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_personne.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_personne.updateAt ? picto_personne.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_personnes_index') }}">Retour à la liste des PictoPersonnes</a>

    <a href="{{ path('app_picto_personnes_new') }}">Create new PictoPersonnes</a>

    {# <a href="{{ path('app_picto_personnes_edit', {'id': picto_personne.id}) }}">edit</a>

    {{ include('picto_personnes/_delete_form.html.twig') }} #}
{% endblock %}
