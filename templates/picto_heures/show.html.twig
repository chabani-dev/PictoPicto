{% extends 'base.html.twig' %}

{% block title %}PictoHeures{% endblock %}

{% block body %}
    <h1>PictoHeures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_heure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_heure.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_heure.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_heure.updateAt ? picto_heure.updateAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_heures_index') }}">Retour à la liste des PictoHeures</a>

     <a href="{{ path('app_picto_heures_new') }}">Create new PictoHeures </a>

    {# <a href="{{ path('app_picto_heures_edit', {'id': picto_heure.id}) }}">edit</a>

    {{ include('picto_heures/_delete_form.html.twig') }} #}
{% endblock %}
