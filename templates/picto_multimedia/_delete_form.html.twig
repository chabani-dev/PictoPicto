<form method="post" action="{{ path('app_picto_multimedia_delete', {'id': picto_multimedia.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_multimedia.id) }}">
    <button class="btn">Delete</button>
</form>
