{% extends 'base.html.twig' %}

{% block title %}PictoTransports{% endblock %}

{% block body %}
    <h1>PictoTransports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_transport.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_transport.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_transport.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_transport.updateAt ? picto_transport.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_transports_index') }}">Retour à la liste des PictoTransports</a>

    <a href="{{ path('app_picto_transports_new') }}">Create new PictoTransports</a>

    {# <a href="{{ path('app_picto_transports_edit', {'id': picto_transport.id}) }}">edit</a>

    {{ include('picto_transports/_delete_form.html.twig') }} #}
{% endblock %}
