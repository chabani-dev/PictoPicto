{% extends 'base.html.twig' %}

{% block title %}PictoCorpsHumain{% endblock %}

{% block body %}
    <h1>PictoCorpsHumain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_corps_humain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_corps_humain.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_corps_humain.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_corps_humain.updateAt ? picto_corps_humain.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_corps_humain_index') }}">Retour à la liste des PictoCorpsHumain</a>

    <a href="{{ path('app_picto_corps_humain_new') }}">Create new PictoCorpsHumain</a>

    {# {{ include('picto_corps_humain/_delete_form.html.twig') }} #}
{% endblock %}
