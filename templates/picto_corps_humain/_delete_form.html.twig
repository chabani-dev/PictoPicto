<form method="post" action="{{ path('app_picto_corps_humain_delete', {'id': picto_corps_humain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_corps_humain.id) }}">
    <button class="btn">Delete</button>
</form>
