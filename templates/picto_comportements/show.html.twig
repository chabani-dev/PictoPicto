{% extends 'base.html.twig' %}

{% block title %}PictoComportements{% endblock %}

{% block body %}
    <h1>PictoComportements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_comportement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_comportement.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_comportement.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_comportement.updateAt ? picto_comportement.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_comportements_index') }}">back to list</a>

    <a href="{{ path('app_picto_comportements_new') }}">Create new PictoComportements</a>

    {# {{ include('picto_comportements/_delete_form.html.twig') }} #}
{% endblock %}
