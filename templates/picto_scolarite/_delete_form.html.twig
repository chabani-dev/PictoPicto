<form method="post" action="{{ path('app_picto_scolarite_delete', {'id': picto_scolarite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_scolarite.id) }}">
    <button class="btn">Delete</button>
</form>
