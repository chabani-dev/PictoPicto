<form method="post" action="{{ path('app_picto_emotions_delete', {'id': picto_emotion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_emotion.id) }}">
    <button class="btn">Delete</button>
</form>
