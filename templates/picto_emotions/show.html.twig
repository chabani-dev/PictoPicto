{% extends 'base.html.twig' %}

{% block title %}PictoEmotions{% endblock %}

{% block body %}
    <h1>PictoEmotions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_emotion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_emotion.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_emotion.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_emotion.updateAt ? picto_emotion.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_emotions_index') }}">Retour à la liste des PictoEmotions</a>

     <a href="{{ path('app_picto_emotions_new') }}">Create new PictoEmotions</a>

    {# <a href="{{ path('app_picto_emotions_edit', {'id': picto_emotion.id}) }}">edit</a>

    {{ include('picto_emotions/_delete_form.html.twig') }} #}
{% endblock %}
