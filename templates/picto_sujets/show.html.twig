{% extends 'base.html.twig' %}

{% block title %}PictoSujets{% endblock %}

{% block body %}
    <h1>PictoSujets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_sujet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_sujet.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_sujet.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_sujet.updateAt ? picto_sujet.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_sujets_index') }}">Retour à la liste des PictoSujets</a>

     <a href="{{ path('app_picto_sports_new') }}">Create new PictoSports</a>

    {# <a href="{{ path('app_picto_sujets_edit', {'id': picto_sujet.id}) }}">edit</a>

    {{ include('picto_sujets/_delete_form.html.twig') }} #}
{% endblock %}
