<form method="post" action="{{ path('app_picto_autres_mots_delete', {'id': picto_autres_mot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_autres_mot.id) }}">
    <button class="btn">Delete</button>
</form>
