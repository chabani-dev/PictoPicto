{% extends 'base.html.twig' %}

{% block title %}PictoSecuriteRoutiere{% endblock %}

{% block body %}
    <h1>PictoSecuriteRoutiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_securite_routiere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_securite_routiere.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_securite_routiere.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_securite_routiere.updateAt ? picto_securite_routiere.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_securite_routiere_index') }}">Create new PictoScolarite</a>

      <a href="{{ path('app_picto_securite_routiere_new') }}">Create new PictoSecuriteRoutiere </a>

    {# <a href="{{ path('app_picto_securite_routiere_edit', {'id': picto_securite_routiere.id}) }}">edit</a>

    {{ include('picto_securite_routiere/_delete_form.html.twig') }} #}
{% endblock %}
