<form method="post" action="{{ path('app_picto_securite_routiere_delete', {'id': picto_securite_routiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_securite_routiere.id) }}">
    <button class="btn">Delete</button>
</form>
