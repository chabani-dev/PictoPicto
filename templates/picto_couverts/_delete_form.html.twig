<form method="post" action="{{ path('app_picto_couverts_delete', {'id': picto_couvert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_couvert.id) }}">
    <button class="btn">Delete</button>
</form>
