{% extends 'base.html.twig' %}

{% block title %}PictoObjets{% endblock %}

{% block body %}
    <h1>PictoObjets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_objet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_objet.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_objet.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_objet.updateAt ? picto_objet.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_objets_index') }}">Retour à la liste des PictoObjets</a>

    <a href="{{ path('app_picto_objets_new') }}">Create new PictoObjets</a>

    {# <a href="{{ path('app_picto_objets_edit', {'id': picto_objet.id}) }}">edit</a>

    {{ include('picto_objets/_delete_form.html.twig') }} #}
{% endblock %}
