{% extends 'base.html.twig' %}

{% block title %}PictoChiffres{% endblock %}

{% block body %}
    <h1>PictoChiffres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_chiffre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_chiffre.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_chiffre.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_chiffre.updateAt ? picto_chiffre.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_chiffres_index') }}">Retour à la liste des PictoChiffres</a>

    <a href="{{ path('app_picto_chiffres_new') }}">Create new PictoChiffres</a>

    {# {{ include('picto_chiffres/_delete_form.html.twig') }} #}
{% endblock %}
