<form method="post" action="{{ path('app_picto_langues_des_signes_delete', {'id': picto_langues_des_signe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_langues_des_signe.id) }}">
    <button class="btn">Delete</button>
</form>
