{% extends 'base.html.twig' %}

{% block title %}PictoLanguesDesSignes{% endblock %}

{% block body %}
    <h1>PictoLanguesDesSignes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_langues_des_signe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_langues_des_signe.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_langues_des_signe.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_langues_des_signe.updateAt ? picto_langues_des_signe.updateAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_langues_des_signes_index') }}">Retour à la liste des PictoLanguesDesSignes</a>

     <a href="{{ path('app_picto_langues_des_signes_new') }}">Create new PictoLanguesDesSignes</a>

    {# <a href="{{ path('app_picto_langues_des_signes_edit', {'id': picto_langues_des_signe.id}) }}">edit</a>

    {{ include('picto_langues_des_signes/_delete_form.html.twig') }} #}
{% endblock %}
