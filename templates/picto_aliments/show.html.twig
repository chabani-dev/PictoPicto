{% extends 'base.html.twig' %}

{% block title %}PictoAliments{% endblock %}

{% block body %}
    <h1>PictoAliments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_aliment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_aliment.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_aliment.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_aliment.updateAt ? picto_aliment.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_aliments_index') }}"> Retour à la liste des PictoAliments</a>

     <a href="{{ path('app_picto_aliments_new') }}">Create new PictoAliments</a>

    {# {{ include('picto_aliments/_delete_form.html.twig') }} #}
{% endblock %}
