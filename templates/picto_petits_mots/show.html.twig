{% extends 'base.html.twig' %}

{% block title %}PictoPetitsMots{% endblock %}

{% block body %}
    <h1>PictoPetitsMots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_petits_mot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_petits_mot.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_petits_mot.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_petits_mot.updateAt ? picto_petits_mot.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_petits_mots_index') }}">Retour à la liste des PictoPetitsMots</a>

     <a href="{{ path('app_picto_petits_mots_new') }}">Create new PictoPetitsMots</a>

    {# <a href="{{ path('app_picto_petits_mots_edit', {'id': picto_petits_mot.id}) }}">edit</a>

    {{ include('picto_petits_mots/_delete_form.html.twig') }} #}
{% endblock %}
