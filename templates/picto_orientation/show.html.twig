{% extends 'base.html.twig' %}

{% block title %}PictoOrientation{% endblock %}

{% block body %}
    <h1>PictoOrientation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_orientation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_orientation.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_orientation.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_orientation.updateAt ? picto_orientation.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_orientation_index') }}">Retour à la liste des PictoOrientation</a>

    <a href="{{ path('app_picto_orientation_new') }}">Create new PictoOrientation</a>

    {# <a href="{{ path('app_picto_orientation_edit', {'id': picto_orientation.id}) }}">edit</a>

    {{ include('picto_orientation/_delete_form.html.twig') }} #}
{% endblock %}
