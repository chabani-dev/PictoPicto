<form method="post" action="{{ path('app_picto_journees_delete', {'id': picto_journee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_journee.id) }}">
    <button class="btn">Delete</button>
</form>
