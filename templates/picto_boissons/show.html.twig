{% extends 'base.html.twig' %}

{% block title %}PictoBoissons{% endblock %}

{% block body %}
    <h1>PictoBoissons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picto_boisson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picto_boisson.name }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ picto_boisson.filename }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ picto_boisson.updateAt ? picto_boisson.updateAt|date('Y-m-d ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picto_boissons_index') }}">Retour à la liste des PictoBoissons</a>

    <a href="{{ path('app_picto_boissons_new') }}">Create new PictoBoissons</a>

    {# {{ include('picto_boissons/_delete_form.html.twig') }} #}
{% endblock %}
