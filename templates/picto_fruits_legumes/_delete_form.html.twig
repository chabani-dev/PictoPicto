<form method="post" action="{{ path('app_picto_fruits_legumes_delete', {'id': picto_fruits_legume.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picto_fruits_legume.id) }}">
    <button class="btn">Delete</button>
</form>
